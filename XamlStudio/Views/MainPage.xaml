<Page
    x:Class="XamlStudio.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ex="using:Microsoft.Toolkit.Uwp.UI.Extensions"
    xmlns:exf="using:Microsoft.Toolkit.Uwp.UI.Extensions.Future"
    xmlns:future="using:Microsoft.Toolkit.Uwp.UI.Controls.Future"
    xmlns:local="using:XamlStudio.Views"
    xmlns:models="using:XamlStudio.Models"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ms-appx:///Microsoft.Toolkit.Uwp.UI/Extensions/NavigationView/Styles/VSCodeActivityBarStyle.xaml" />
                <ResourceDictionary Source="ms-appx:///Microsoft.Toolkit.Future/Extensions/Pivot/Styles/MSEdgeTabStyle.xaml" />
                <ResourceDictionary Source="ms-appx:///Microsoft.Toolkit.Future/Extensions/Pivot/Styles/MSEdgePivotStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="48"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <NavigationView x:Name="NavMenu"
                        Style="{StaticResource VSCodeActivityBarStyle}"
                        MenuItemContainerStyle="{StaticResource LargeIconNavigationViewItemStyle}"
                        exf:NavigationViewExtensions.SelectedIndex="0"
                        exf:NavigationViewExtensions.CollapseOnClick="True">
            <NavigationView.MenuItems>
                <NavigationViewItem x:Uid="MainPage_NavMenu_Explorer">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8C8;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Uid="MainPage_NavMenu_ImageSources">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEB9F;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Uid="MainPage_NavMenu_DataSources">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEE94;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Uid="MainPage_NavMenu_Debug">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEBE8;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Uid="MainPage_NavMenu_Toolbox">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE821;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem x:Uid="MainPage_NavMenu_Search">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE094;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <Grid x:Name="ContentGrid" MinWidth="200" Width="300"
                  BorderBrush="DarkGray"
                  BorderThickness="0,0,1,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <future:ContentSizer Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Stretch" Width="3" Background="DarkGray">
                    <TextBlock/>
                </future:ContentSizer>

                <future:SwitchPresenter Value="{Binding ElementName=NavMenu, Path=SelectedItem.Content}">
                    <future:Case Value="{x:Bind exf:Bind.LocalizedString('MainPage_NavMenu_Explorer/Content')}">
                        <local:Explorer MainViewModel="{x:Bind ViewModel}"/>
                    </future:Case>
                    <future:Case Value="{x:Bind exf:Bind.LocalizedString('MainPage_NavMenu_Debug/Content')}">
                        <local:Binding MainViewModel="{x:Bind ViewModel}"/>
                    </future:Case>
                    <future:Case Value="Settings"><!-- It seems NavigationView always has this as 'Settings'? -->
                        <local:SettingsPanelPage MainViewModel="{x:Bind ViewModel}"/>
                    </future:Case>
                    <future:Case IsDefault="True">
                        <Border Background="DarkGray">
                            <TextBlock HorizontalAlignment="Left"
                                        VerticalAlignment="Center"
                                        Margin="16,0,0,0"
                                        Foreground="Black"
                                        Text="{Binding ElementName=NavMenu, Path=SelectedItem.Tag}" />
                        </Border>
                    </future:Case>
                </future:SwitchPresenter>
            </Grid>
        </NavigationView>

        <Pivot x:Name="DocumentTabsPivot"
               Grid.Column="1"
               SelectionChanged="Pivot_SelectionChanged"
               ItemsSource="{x:Bind ViewModel.OpenFiles}"
               Style="{StaticResource MSEdgePivotStyle}"
               DataContext="{x:Bind ViewModel}"
               exf:PivotExtensions.CloseButtonCommand="{Binding CloseActiveDocumentCommand}"
               exf:PivotExtensions.PivotHeaderItemStyle="{StaticResource MSEdgeTabStyle}"
               exf:PivotExtensions.IsContentVisible="False">
            <Pivot.HeaderTemplate>
                <DataTemplate x:DataType="models:XamlDocument">
                    <TextBlock Text="{x:Bind Title}"/>
                </DataTemplate>
            </Pivot.HeaderTemplate>
            <Pivot.RightHeader>
                <Button Style="{StaticResource MSEdgePivotButtonStyle}"
                        Command="{x:Bind ViewModel.NewDocumentCommand}">
                    <SymbolIcon Symbol="Add" exf:SymbolIconExtensions.FontSize="12" />
                </Button>
            </Pivot.RightHeader>
        </Pivot>
            
        <future:SwitchPresenter x:Name="MainContent"
                                Margin="0,32,0,0"
                                Grid.Column="1"
                                Value="{x:Bind ViewModel.ActiveFile.IsWelcome, Mode=OneWay}"
                                TargetType="x:Boolean">
            <future:Case Value="True">
                <Grid>
                    <StackPanel Padding="8">
                        <TextBlock FontFamily="Segoe UI" FontSize="32" FontStyle="Italic" FontWeight="SemiLight" FontStretch="UltraCondensed">&lt;XAML/&gt;</TextBlock>
                        <TextBlock Margin="4,-12,0,0" FontFamily="Segoe UI" FontSize="32" FontWeight="ExtraBlack" FontStretch="UltraExpanded">STUDIO</TextBlock>
                    </StackPanel>
                    <TextBlock FontSize="16"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center">Welcome to XAML Studio!</TextBlock>
                </Grid>
            </future:Case>
            <future:Case Value="False">
                <local:Document x:Name="Document" LoadedDocument="{x:Bind ViewModel.ActiveFile, Mode=OneWay}" MainViewModel="{x:Bind ViewModel}" />
            </future:Case>
        </future:SwitchPresenter>
    </Grid>
</Page>
