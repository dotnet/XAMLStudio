<Page
    x:Class="XamlStudio.Views.WelcomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XamlStudio.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:storage="using:Windows.Storage"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:ic="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <StackPanel Padding="16">
                <TextBlock FontFamily="Segoe UI" FontSize="32" FontStyle="Italic" FontWeight="SemiLight" FontStretch="UltraCondensed">&lt;XAML/&gt;</TextBlock>
                <TextBlock Margin="4,-12,0,0" FontFamily="Segoe UI" FontSize="32" FontWeight="ExtraBlack" FontStretch="UltraExpanded">STUDIO</TextBlock>
            </StackPanel>
            <TextBlock FontSize="16" Padding="16"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Center">For Rapid Prototyping</TextBlock>
        </StackPanel>
        <StackPanel Grid.Row="1" Padding="12">
            <TextBlock FontSize="24" FontWeight="Light">Get Started</TextBlock>
            <StackPanel Padding="12">
                <HyperlinkButton Command="{x:Bind MainViewModel.NewDocumentCommand}">New file</HyperlinkButton>
                <HyperlinkButton Command="{x:Bind MainViewModel.OpenDocumentCommand}">Open file...</HyperlinkButton>
            </StackPanel>
        </StackPanel>
        <StackPanel Grid.Row="2">
            <TextBlock FontSize="24" FontWeight="Light" Padding="12">Recent Files</TextBlock>
            <ItemsControl x:Name="RecentFileList"
                          Margin="24,0,0,0"
                          DataContext="{x:Bind}"
                          ItemsSource="{x:Bind RecentFiles}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="storage:StorageFile">
                        <HyperlinkButton Content="{x:Bind DisplayName}" ToolTipService.ToolTip="{x:Bind Path}"
                                         Command="{Binding DataContext.MainViewModel.OpenFileCommand, ElementName=RecentFileList}"
                                         CommandParameter="{x:Bind}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</Page>
